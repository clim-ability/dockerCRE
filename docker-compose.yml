proxy2:
# image: nginx:latest
  build: .
  dockerfile: proxy/Dockerfile
  container_name: proxy2
  volumes:
    - ./proxy/certs:/etc/nginx/certs:ro
    - ./proxy/nginx.conf:/etc/nginx/conf.d/default.conf
    - /etc/letsencrypt:/etc/letsencrypt
  ports:
    - "80:80"
    - "443:443"
  links:
    - nginx2

nginx2:
    image: nginx:latest
    container_name: nginx2
    volumes:
        - ./nginx2/html:/code
        - ./nginx2/www.example.com.conf:/etc/nginx/conf.d/www.example.com.conf
    links:
        - php2

php2:
  build: .
  dockerfile: php/Dockerfile
  container_name: php2
  volumes:
    - ./nginx2/html:/code
    - ./php/php-ini-overrides.ini:/etc/php/7.0/fpm/conf.d/99-overrides.ini
#  links:
#    - memcached2
#    - postgres2
#    - elasticsearch2




